{
  "manifest_version": 3,
  "name": "Lodestar Bookmarks (Claude Code)",
  "version": "1.1",
  "description": "Helper for the Claude Code skill lodestar:bookmarks. It lets Claude search, file and tidy your Chrome bookmarks — running only the bookmark actions Claude sends over a local connection on this computer. Nothing leaves the machine.",
  "permissions": ["bookmarks", "alarms"],
  "background": { "service_worker": "background.js" }
}
