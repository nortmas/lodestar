{
  "manifest_version": 3,
  "name": "Bookmark Agent Bridge",
  "version": "1.1",
  "description": "Executes bookmark operations sent by the local bm.py bridge over a localhost WebSocket. No data leaves the machine.",
  "permissions": ["bookmarks", "alarms"],
  "background": { "service_worker": "background.js" }
}
